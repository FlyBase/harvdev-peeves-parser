
## ! AB8.   Is AB1a used/usable as a balancer? (y/n)            *u :

### Multiple values allowed:

No (Implemented)

### !c Applicable:

Yes (Implemented)

### Inclusion essential:

Yes (Implemented)

### Mandatorially has value:

If there is no !c, this field must be filled in  ('contains_data' subroutine in loop where the field is identified).

If there is a !c, this field is allowed to be empty (this allows for future correction of any existing AB8 data if we have not yet identified all the existing errors in the database).


### Dupl. for multiple field:

No (Implemented)


### Checks:

Checks within field:

* 'check_single_allowed_value' checks that any value corresponds to the single value ('y') allowed for this field

### Related fields:



### Comments:



### Status:



### Updated:

gm260608.
