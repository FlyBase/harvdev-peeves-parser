
## ! SF4d. Sequence accession (AC no. | gi no. | date)[dupl for multiple) :

### Multiple values allowed:

No (implemented)

### !c Applicable:

Yes (implemented)


### Inclusion essential:

No (implemented)


### Mandatorially has value:

No (implemented)


### Dupl. for multiple field:

Yes (implemented) SF4d -> SF4e, SF4f


### Checks:



Checks between fields:

 * sub 'no_hashes_in_proforma'- checks that there are no hashes in the entire proforma if SF4d is filled in (this is to make it easier to implement checking of fields that can be duplicated).


* If SF4e is filled in, SF4d must be filled in.

* If SF4f is filled in, SF4d must be filled in.


### Related fields:



### Comments:



### Status:

Not yet implemente any format checking of the data within the field

### Updated:

gm160304.
